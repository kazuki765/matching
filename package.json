{
  "name": "matching app",
  "version": "1.0.0",
  "description": "matching platformを自分なりに作ってみる（web）",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky install"
  },
  "author": "kujira765",
  "devDependencies": {
    "husky": "^8.0.3"
  }
}
